package Strings;

public class IsAnagramByMethods {
    public static void main(String[] args) {

             String a="abcd";
             String b="xyza";
                char arr1[]=a.toCharArray();
                char arr2[]=b.toCharArray();
              // Arrays.sort(arr1);
              //  Arrays.sort(arr2);
                a=new String(arr1);
                b=new String(arr2);
                // boolean x=if(a.equals(b));
              //  return a.equals(b);







            }
        }

